## BRIEF
In this lab, you categorize the 8 course red flags into four categories:
- `memory-safety`
- `availability`
- `input-validation`
- `supply-chain`

## GUIDED STEPS

1. Inspect `files/redflags.md`.
2. Create `answers.txt` categorizing each flag:
   ```text
   rf1=availability
   rf2=memory-safety
   rf3=input-validation
   rf4=input-validation
   rf5=availability
   rf6=supply-chain
   rf7=input-validation
   rf8=memory-safety
   ```
3. Create `checklist.md` with sections covering all four categories.
4. Verify with `lab check rust L7.2`.
