{
  "id": "L4.4",
  "title": "Environment attacks — PATH, IFS, and untrusted search paths (CWE-426)",
  "type": "AUDIT",
  "objective": "Find the line that lets an attacker-writable directory be searched before the trusted ones, so a dropped command runs instead of the real one, and pin a safe PATH.",
  "gate": false,
  "est_minutes": 15,
  "teaching_samples": {
    "files/healthcheck.sh": ["SC2009"]
  }
}
