## BRIEF
Identify persistence mechanisms across Windows and Linux: run keys, scheduled tasks, services, and cron.

## GUIDED STEPS

1. Inspect `files/windows-persistence.json`, `files/web01-cron.log`, `files/root-crontab.txt`, and `files/persistence-legend.md`.
2. Fill `answers.txt`. Every line in the template names exactly what it wants —
   answer each one from the evidence above.
3. Verify with `lab check soc L3.4`.
