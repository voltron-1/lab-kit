The wrong child is an ancestry problem: a document, browser, or mail client spawning a shell or interpreter is the tell, not the child process by itself.
Define normal first — what should launch powershell, cmd, mshta — so "wrong" is a rule, not a hunch.
This is ATT&CK Execution: the same powershell.exe is benign under an admin parent and malicious under WINWORD.EXE.
