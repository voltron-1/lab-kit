The Linux auth story reads in order: many Failed password from one source, an Accepted from that same source, then what the session did.
A new user with UID 0 is a root backdoor, and a sudo COMMAND line names the attacker's next action.
Read source and method, not just the word Accepted — a publickey login from an internal host is the benign control beside the brute force.
