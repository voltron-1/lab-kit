Extensions lie — magic bytes (PK, MZ, ffd8) give the true type, and a double extension like .pdf.exe is a classic disguise.
Office files ending .docm/.xlsm carry macros inside a ZIP container; that vbaProject is the risk, not the icon.
The hash is the sample's identity and the safe first step — inspect and enrich, never open or detonate.
