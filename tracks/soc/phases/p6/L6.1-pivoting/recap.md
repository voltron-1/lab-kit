Pivoting turns one indicator into the whole story by following join keys: user→host, host→process (ProcessGuid), process→network (uid), host→persistence.
One alert is a single beat; the pivot reveals scope, persistence, and lateral movement the alert never mentioned.
The compromised credential is the thread — follow it to the second host, and the incident is bigger than the queue showed.
