## BRIEF
Match incident communications to audience (`tier2 | user | mgmt | handoff`) and timing (`immediate | eos | routine`).

## GUIDED STEPS

1. Inspect `files/comms-cases.md` and `files/charter-comms.md`.
2. Copy `files/answers.template.txt` to `answers.txt`.
3. Fill `answers.txt`:
   - `q1a` (case 1 audience) -> `tier2`
   - `q1t` (case 1 timing) -> `immediate`
   - `q2a` (case 2 audience) -> `handoff`
   - `q2t` (case 2 timing) -> `eos`
   - `q3a` (case 3 audience) -> `user`
   - `q3t` (case 3 timing) -> `routine`
   - `q4a` (case 4 audience) -> `handoff`
   - `q4t` (case 4 timing) -> `eos`
   - `q5` (essential item category for in-progress cases in handoff) -> `watch`
4. Run `lab check soc L6.5`.
