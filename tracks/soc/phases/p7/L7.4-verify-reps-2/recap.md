The grounding contract: every AI claim must cite an event id that exists in the evidence — no real id, no claim.
Three ways it fails: a fabricated id (hallucination), a real id used for the wrong connection (wrong pivot), and a confident claim with no citation (ungrounded).
This is the exact contract your architecture holds its own agents to — verifying it by hand is the skill the whole course was built to teach.
