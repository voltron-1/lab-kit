# Enrichment — reported email
- Sender domain `copperm1ne-billing[.]example` registered 2026-03-08 (3 days before delivery).
- Org domain is `coppermine[.]example`; the sender swaps `i` for `1`.
- Sending IP `198.51.100[.]71` is the same source seen in the 2026-03-11 spray.
- Attachment `invoice_2026-03.docm` is macro-enabled.
