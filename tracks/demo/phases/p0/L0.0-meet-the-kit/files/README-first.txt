Welcome. Everything you do in this lab happens in this directory —
workspace/demo/L0.0/ — and the grader will refuse to look anywhere else.

Kit facts you'll be quizzed on:
  marks: ✓ passed · ○ not done · ⏭ forced (--force) — ⏭ never becomes ✓
  state: .progress.json at the repo root, written atomically (Ctrl-C safe)
  hints: exactly 3 levels per lab, one per 'lab hint' invocation
  resume: replays your last recap card, then names the next lab
